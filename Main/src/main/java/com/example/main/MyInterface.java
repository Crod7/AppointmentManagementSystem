package com.example.main;
/** This interface holds the functionality for the LAMBDA expressions.
 */
@FunctionalInterface
public interface MyInterface {
    /** This method is used to run the LAMBDA expressions throughout the program. Once initialized
     * the program can define inside the LAMBDA expression each specific task that particular
     * LAMBDA expression will do.
     */
    public void run();
}
